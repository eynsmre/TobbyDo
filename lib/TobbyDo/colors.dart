import 'package:flutter/material.dart';

class ColorsUtility {
  static const black = Colors.black;
  static const red = Colors.red;
  static const white = Colors.white;
  static const blue = Colors.blue;
}

class PaddingUtility {
  static const xsmalltopOnly = EdgeInsets.only(top: 7.5);
  static const xsmallbottomOnly = EdgeInsets.only(bottom: 7.5);
  static const xsmallV = EdgeInsets.symmetric(vertical: 10);
  static const xsmallH = EdgeInsets.symmetric(horizontal: 7.5);
  static const xsmallHV = EdgeInsets.symmetric(horizontal: 7.5, vertical: 7.5);
  static const smallHV = EdgeInsets.symmetric(horizontal: 15.0, vertical: 15.0);
  static const smallH = EdgeInsets.symmetric(horizontal: 15.0);
  static const normalHV = EdgeInsets.symmetric(horizontal: 30.0, vertical: 30.0);
  static const normalH = EdgeInsets.symmetric(horizontal: 30.0);
  static const titlePadding = EdgeInsets.symmetric(vertical: 30.0);
}
